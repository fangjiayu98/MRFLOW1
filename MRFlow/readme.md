运行所有baseline:

1. 对于air数据集, 在终端配好环境输入bash /scripts/air_run.sh
2. 对于energy,在终端配好环境输入 bash /scripts/energy_wind_run.sh
3. 对于pems,在终端配好环境输入 bash /scripts/pems_run.sh

如果单独运行我们的模型，你可以运行 scripts脚本里具体到mrflow相关的py文件
